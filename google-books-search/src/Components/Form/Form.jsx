const Form = () => {
    return (
        <div>
            <form>
                <input type="text" placeholder="Search for your book" />
                <button>Search</button>
            </form>
        </div>
    );
};

export default Form;
